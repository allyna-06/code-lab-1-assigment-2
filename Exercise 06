# Stored password that the user must enter to gain access
stored_password = "12345"

# Using a while loop to repeatedly prompt the user for the password until the correct one is entered
while True:
    # Prompting the user to input the password
    user_input = input("Enter the password: ")

    # Check if the entered password matches the stored correct password
    if user_input == stored_password:
        # If correct, grant access and display success message
        print("ACCESS GRANTED! The password is correct.")
        break  # Exit the loop once the correct password is entered

    else:
        # If incorrect, inform the user and prompt again
        print("Incorrect password! Please try again.")
